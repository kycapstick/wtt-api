{
  "name": "where-they-to",
  "version": "1.0.0",
  "description": "API backend for Where-They-To",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "cd migrations && node all-migrations.js"
  },
  "author": "kycapstick",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "nodemon": "^2.0.7"
  }
}
